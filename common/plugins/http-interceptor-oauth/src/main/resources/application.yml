fit:
  beans:
    packages:
    - 'modelengine.jade.common.filter'

filter:
  static-resource-filter: 'enable'

authorization-server:
  jwk-endpoint: http://oauth.server.local/oauth2/jwks
  api-endpoint: http://oauth.client.local/api
